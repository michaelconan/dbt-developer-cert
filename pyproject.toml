[project]
name = "dbt-developer-cert"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=2.0.0a3",
]

[dependency-groups]
dev = [
    "sqlfluff>=4.2.2",
    "sqlfluff-templater-dbt>=4.2.2",
]
